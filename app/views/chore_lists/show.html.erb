<H2>
    <%=@user.username%>'s assignments
    <!-- now show the tasks. -->
    
    <% @user.chore_lists.each do |cl| %>
        <h4><%= cl.name %>
        - <%= link_to "Edit", edit_chore_list_path(cl) %></h4>
        <ul>
            <% cl.tasks.each do |t| %>
                <li><%=t.name%><br /><br />
                    <%= t.details%><br />
                    ["Mark Task Complete Button"]
            <% end %>
        </ul>

    <% end %>
    
    
    <%# Loop through @user.chore_list.each do |cl| { 
        During this loop iteration, display chore_list.tasks.each {
            display task t.name, t.details
            buttons.

        }
    } %>
</H2>