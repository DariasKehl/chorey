<h2>All Chore Lists (Index)</h2>

<% @chore_lists.each do |cl| %>
    <%= link_to User.find_by_id(cl.user_id).username, chore_list_path(cl) %> 
    assigned to: 
    <%= link_to "NameForLink", user_path(cl.user_id) %>  <br />
<% end %>